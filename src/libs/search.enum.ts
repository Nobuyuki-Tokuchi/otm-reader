export enum SearchType {
    WORD,
    TRANSLATION,
    ALL,
    TAG,
    TRANSLATION_TITLE,
    VARIATION_TITLE,
}

export enum MatchType {
    FORWARD,
    BACKWARD,
    PARTIAL,
    EXACT,
    NOT,
}
